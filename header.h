#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>

void stringcpy(char *str, char *a, int n); // 문자열 str을 a에 복사하는 함수
char *triv(char* str, int d, int n);
char *juggling(char *arr,int N, int d);
char *block_swap(char *str, int n, int d);
char *reversal(char *str, int n, int d);
